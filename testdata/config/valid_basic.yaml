version: 1
profiles:
  dev:
    vars:
      DATABASE_URL:
        type: literal
        value: postgres://localhost/myapp
      API_KEY:
        type: ref
        uri: env://LOCAL_API_KEY
        sensitive: true
